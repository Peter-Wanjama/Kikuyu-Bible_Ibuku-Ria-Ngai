import { Text, View } from "react-native";

export default function SavedScreen(){
return(
    <View styles={{flex:1,justifyContent:'center',alignItems:'center'}}>
        <Text>Saved Screen and Bookmarks</Text>
    </View>
    );
}